<!-- Begin Page Content -->
<div class="container-fluid">

    <!-- Page Heading -->
    <div class="row mb-3">
        <div class="col">
            <h1 class="h3 mb-2 text-gray-800"><%= t('shared_book.incomes_expenses_edit') %></h1>
        </div>
        <div class="col text-right">
            <%= link_to t('shared_book.incomes_expenses_index'), incomes_expenses_path, class: "btn btn-primary" %>
        </div>
    </div>

  <!-- New Data -->
  <div class="card shadow mb-4">
    <div class="card-body">
      <%= render "form", incomes_expense: @incomes_expense %>
    </div> 
  </div>
</div>
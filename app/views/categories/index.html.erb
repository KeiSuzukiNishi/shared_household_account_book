<h1><%= t('shared_book.category') %></h1>

<div id="categories">
  <% @categories.each do |category| %>
    <%= render category %>
  <% end %>
</div>

<%= link_to t('shared_book.new_category'), new_category_path %>